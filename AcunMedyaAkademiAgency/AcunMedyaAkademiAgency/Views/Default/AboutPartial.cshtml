@model List<AcunMedyaAkademiAgency.Entities.About>
@{
    var count = 2;
}
<section class="page-section" id="about">
    <div class="container">
        <div class="text-center">
            <h2 class="section-heading text-uppercase">Hakkımızda</h2>
            <h3 class="section-subheading text-muted">Bizi Daha Yakından Tanıyın</h3>
        </div>
        <ul class="timeline">
            @foreach (var item in Model)
            {
                if (count % 2 == 0)
                {
                    <li>
                        <div class="timeline-image"><img class="rounded-circle" src="@item.ImageUrl" style="width: 100%;height: 100%;object-fit: cover;" /></div>
                        <div class="timeline-panel">
                            <div class="timeline-heading">
                                <h4>@item.aboutDate</h4>
                                <h4 class="subheading">@item.Title</h4>
                            </div>
                            <div class="timeline-body"><p class="text-muted">@item.Description</p></div>
                        </div>
                    </li>
                }

                else
                {
                    <li class="timeline-inverted">
                        <div class="timeline-image"><img class="rounded-circle" src="@item.ImageUrl" width="160px" height="160px" /></div>
                        <div class="timeline-panel">
                            <div class="timeline-heading">
                                <h4>@item.aboutDate</h4>
                                <h4 class="subheading">@item.Title</h4>
                            </div>
                            <div class="timeline-body"><p class="text-muted">@item.Description</p></div>
                        </div>
                    </li>
                }
                count++;
            }
        </ul>
    </div>
</section>
